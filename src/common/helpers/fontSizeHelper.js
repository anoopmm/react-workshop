// import { Dimensions, PixelRatio } from 'react-native';

// export default fontSizeInDp = fontPercentage => {
//   const screenWidth = Dimensions.get('window').width;
//   const fontSize = parseFloat(fontPercentage);
//   return PixelRatio.roundToNearestPixel((screenWidth * fontSize) / 100);
// };
// export { fontSizeInDp };
